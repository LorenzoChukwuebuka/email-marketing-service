backend/
├── cmd/
│   ├── server/
│   │   └── main.go               # Entry point (move server.go here)
│   └── worker/
│       └── main.go               # For background jobs
├── api/
│   ├── v1/
│   │   ├── controllers/          # Keep this
│   │   ├── middleware/           # Move from current middleware
│   │   ├── routes/               # Move from current routes
│   │   └── dto/                  # Move DTOs here, version-specific
│   └── v2/                       # Future version
│       └── ...
├── internal/
│   ├── models/                   # Core domain models
│   ├── repositories/             # Database interaction
│   ├── services/                 # Business logic
│   ├── imap/                     # Move from current internals
│   ├── smtp/                     # Move from current internals
│   └── workers/                  # Move from current internals
├── pkg/
│   ├── asyncpkg/                 # Keep as is
│   ├── database/                 # Move from api/v1
│   ├── factory/                  # Move from api/v1 if generic
│   └── utils/                    # Move from api/v1
├── migrations/                   # Database migrations
├── templates/                    # Move from api/v1
├── uploads/                      # Keep as is
└── configs/                      # Configuration files